apiVersion: v1
kind: Service
metadata:
  name: aspnet-service
spec:
  type: NodePort
  selector:
    app: aspnet-app 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080